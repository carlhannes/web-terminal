# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local
# …but the eval reverse-proxy config is committed despite matching *.local
!deploy/Caddyfile.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Self-signed certs generated by deploy/run-local.sh
deploy/certs/

# Playwright CLI local-install artifacts (regenerable via `playwright-cli install --skills`;
# browser binaries live in ~/.cache/ms-playwright, outside the repo)
.playwright/
.playwright-cli/
.claude/skills/playwright-cli/
